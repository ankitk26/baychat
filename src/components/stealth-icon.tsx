import { DetectiveIcon } from "@phosphor-icons/react";

// The Stealth provider has no public branding yet, so we use an
// incognito-style icon to represent it.
export default function StealthIcon() {
	return <DetectiveIcon className="size-4" />;
}
